import { Container, Typography } from "@mui/material";

export default function CommitmentPage() {

    return (<Container maxWidth="md">
        <Typography gutterBottom>
            如果读者熟悉哈希函数，应该能意识到消息的哈希其实是一个看上去很有诱惑力的选择。
            下面还没写。
        </Typography>
    </Container >
    )
}